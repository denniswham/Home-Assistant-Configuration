---
# The history component will track everything that is going on within
# Home Assistant and allows the user to browse through it.
#
# https://www.home-assistant.io/components/history/
#
history:

  use_include_order: True
  include:
    entities:
      - person.dennis
      - person.stephanie
      - person.lucas
      - person.timo
      - alarm_control_panel.Konnected_Alarm
      - sensor.retsinagaard_eta
      - sensor.dennis_to_home
      - sensor.stephanie_to_home
      - proximity.home